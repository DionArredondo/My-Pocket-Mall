import 'package:flutter/material.dart';

class menuGps extends StatefulWidget {
  const menuGps({super.key});

  @override
  State<menuGps> createState() => _menuGpsState();
}

class _menuGpsState extends State<menuGps> {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Bienvenidos"),
    );
  }
}
